
config:
    name: "测试用例-登录RuoYi管理系统"

    variables:
        username: ${ENV(USERNAME)}
        password: ${ENV(PASSWORD)}

    base_url: "http://${ENV(Aliyunhost)}"

    output:
        - token

teststeps:
-
    name: 登录
    api: demo/api/api-Login.yml
#    variables:
#        username: username
#        password: $password
    extract:
        - token: content.token
    validate:
        - eq: ["status_code", 200]
#-
#    name: demo step 2
#    api: path/to/api2.yml
#    variables:
#        token: $token
