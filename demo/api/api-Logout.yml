
name: 退出当前登录用户

variables:
    token: $token

request:
    url: http://${ENV(Aliyunhost)}/prod-api/logout
    method: POST
    headers:
      Content-type: "application/json"
      Authorization: $token

validate:
    - eq: ["status_code", 200]
    - eq: ["json.msg", "退出成功"]
